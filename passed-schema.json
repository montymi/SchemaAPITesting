{
  "type": "object",
  "properties": {
    "user": {
      "type": "object",
      "properties": {
        "id": {"type": "integer"},
        "name": {"type": "string"},
        "email": {"type": "string"},
        "phone": {"type": "string"}
      },
      "required": ["id", "name", "email"]
    },
    "orders": {
      "type": "array"
    }
  },
  "required": ["user", "orders"]
}
// Main Properties: [user, orders]
